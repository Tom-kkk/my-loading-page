/**
 * 站点数据（默认中文，与 useSiteContent 一致；供未接入 Locale 的旧组件复用）
 */
import { siteZh } from '@/locales/buildSite'

export const {
  navLinks,
  hero,
  heroExtra,
  techTags,
  about,
  startSection,
  featuresChessSection,
  featuresGridSection,
  projectsSection,
  projects,
  statsSection,
  contact,
  ctaFooter,
} = siteZh

export default siteZh
